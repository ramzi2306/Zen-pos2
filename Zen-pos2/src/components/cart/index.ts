/**
 * cart/ — shopping cart and checkout components.
 *
 * CartSidebar  Full checkout panel with customer search, receipt, and payment.
 * CartItem     A single swipeable row (drag-to-delete + inline editor).
 * CartFloatingAction  Mobile floating CTA above the bottom nav bar.
 */
export { CartSidebar } from './CartSidebar';
export { SwipeableCartItem } from './CartItem';
export { CartFloatingAction } from './CartFloatingAction';
