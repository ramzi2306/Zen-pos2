/**
 * layout/ — app-shell navigation components.
 *
 * These components form the chrome around every view:
 * the top header, the side navigation, the mobile bottom bar,
 * the profile slide-out panel, and the floating cart button.
 */
export { ProfilePanel } from './ProfilePanel';
export { Sidebar } from './Sidebar';
export { TopBar } from './TopBar';
export { MobileNav } from './MobileNav';
